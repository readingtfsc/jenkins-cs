pipeline {
    agent any

    stages {
        stage('Example') {
        
           
            steps {
                go env
            }
        }
    }
    post {
        always {
            echo 'I will always say H111ello again!'
        }
}
}
