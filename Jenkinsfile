pipeline {
    agent any

    stages {
        stage('Example') {
        
           
            steps {
                echo 'Hello World2,1111112';
                go env
            }
        }
    }
    post {
        always {
            echo 'I will always say H111ello again!'
        }
}
}
